Follow the steps:

1. Open mysql in terminal
2. Create a new database (Optional : but the database should be empty) // create database databaseName;
3. Select the database using 'use' command, // use DatabaseName;
4. Now run "SOURCE /home/[username]/Dropbox/BookBarn/DatabaseRelated/sqlCommands/createCommands.sql"
			     |
			     V			     	
		change your user name here
